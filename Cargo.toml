[package]
name = "maybe-valid"
version = "0.1.0"
edition = "2021"

license = "Apache-2.0 OR MPL-2.0"

repository = "https://github.com/metastable-void/maybe-valid"
readme = "README.md"

[dependencies]

[features]
default = ["std"]
std = ["alloc"]
alloc = []
